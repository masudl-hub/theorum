{
  "name": "@theorum/core",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "THEORUM_TRACE_DIR= deno test --allow-env --allow-read --allow-write --allow-net",
    "coverage": "rm -rf coverage && THEORUM_TRACE_DIR= deno test --allow-env --allow-read --allow-write --allow-net --coverage=coverage tests/ && deno coverage coverage --lcov --output=coverage/lcov.info && node scripts/lcov-to-istanbul.mjs",
    "lint:biome": "biome check .",
    "lint:ast-grep": "ast-grep scan",
    "lint:fallow": "npm run coverage && fallow audit && fallow health && fallow dupes && fallow dead-code",
    "lint": "npm run lint:biome && npm run lint:ast-grep && npm run lint:fallow"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.5.8",
    "@ast-grep/cli": "^0.41.0",
    "fallow": "^0.3.0"
  }
}
