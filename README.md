# Theorum

Agent **kernel**: model catalog, profile registry, resolve, optional tool loop, Interactions provider, traces, quota primitives.

A host registers apps with `registerProfile` and `registerStructured`. This folder does not contain Daily, Vinylator, Studio, Mermaid, or Planner.
